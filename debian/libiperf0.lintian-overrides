# the Debian wiki warns about putting symbol files into
# a package. I'm leaving it out until I have a better
# undestanding of this
libiperf0: no-symbols-control-file usr/lib/*/libiperf.so.0.0.0
# this is lintian experimental and I'm ignoring it for the moment
libiperf0: shlib-calls-exit usr/lib/*/libiperf.so.0.0.0
